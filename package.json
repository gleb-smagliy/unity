{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.13.0"
  },
  "scripts": {
    "before-link": "lerna bootstrap && npm run test && npm run build && npm run prune-dev",
    "test": "lerna run test",
    "build": "lerna run build",
    "prune-dev": "lerna run prune-dev"
  },
  "dependencies": {}
}
